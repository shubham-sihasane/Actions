name: Matrix Workflow

on:
  push:
  workflow_dispatch:

jobs:
  deploy-os:
    strategy:
      fail-fast: true # false
      max-parallel: 2
      matrix:
        os: [ubuntu-latest, ubuntu-24.04, windows-latest] # Can add windows and mac
        images: [hello-world, alpine]
        exclude: 
          - os: [windows-latest]
            images: [hello-world, alpine]
        # include:
        #   - images: [nginx]
        #     os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    steps:
      - name: Docker Info
        run: docker info
      - name: Docker Hello World
        run: docker pull ${{ matrix.images }}
      - name: List Images
        run: docker image ls

# name: Installing Python
# 'on':
#   - push
# jobs:
#   build:
#     runs-on: '${{ matrix.os }}'
#     strategy:
#       matrix:
#         os:
#           - ubuntu-latest
#           - macos-14
#           - windows-latest
#         py-version:
#           - "3.10"
#           - "3.11"
#           - "3.12"
#         exclude: #added
#           - os: macos-14 #added
#             py-version: 3.12 #added
#     steps:
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '${{ matrix.py-version }}'

